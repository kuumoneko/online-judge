import { getGravatarURL, geturl, getdata, replaceAll, cookie, all_language, generateRandomString, allowed_html_tags } from "./@types/index.ts"
import { color, color_themes, get_rank_color } from "./@types/color.js";

export {
    getGravatarURL, geturl, getdata, replaceAll, cookie, all_language, generateRandomString, allowed_html_tags,
}

export {
    color, color_themes, get_rank_color
}
