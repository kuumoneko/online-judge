import React, { useState } from "react";
import { geturl } from "ulti";

export function Problem_submission() {
    const url = geturl();

    const [body, setbody] = useState(<></>)
    const [info, setinfo] = useState(<></>)

    return (
        <div>

        </div>
    )
}